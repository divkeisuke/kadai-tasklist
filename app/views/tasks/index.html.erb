<% if logged_in? %>
<h1>ログイン中のユーザー名：<%= current_user.name %></h1>
<h2>タスク一覧</h2>
<ul>
    <% @tasks.each do |task| %>
    <li><%= link_to task.id,task %>:<%= task.content %>:<%= task.status %></li>
    <% end %>
    <%= paginate @tasks %>
</ul>
<%= link_to"新規タスク作成",new_task_path %>
<% end %>